name: 检查上游标签更新并触发编译仓库

on:
  workflow_dispatch:
  schedule:
    - cron: '0 17 */3 * *'    # 每3天的北京时间01:00运行

jobs:
  check-and-dispatch:
    runs-on: ubuntu-latest
    steps:
      - name: 🔍 获取 ImmortalWrt 最新标签
        id: get_tag
        run: |
          latest_tag=$(curl -s https://api.github.com/repos/immortalwrt/immortalwrt/tags | jq -r '.[0].name')
          echo "latest_tag=$latest_tag" >> "$GITHUB_OUTPUT"

      - name: 📄 读取上次保存的标签
        id: read_tag
        run: |
          CACHE_DIR="$HOME/.repo_cache"
          mkdir -p $CACHE_DIR
          CACHE_FILE="$CACHE_DIR/last_tag"
          if [ -f "$CACHE_FILE" ]; then
            last_tag=$(cat "$CACHE_FILE")
          else
            last_tag=""
          fi
          echo "last_tag=$last_tag" >> "$GITHUB_OUTPUT"

      - name: 🔁 判断标签是否更新
        id: compare
        run: |
          if [ "${{ steps.get_tag.outputs.latest_tag }}" != "${{ steps.read_tag.outputs.last_tag }}" ]; then
            echo "trigger=true" >> "$GITHUB_OUTPUT"
          else
            echo "trigger=false" >> "$GITHUB_OUTPUT"

      - name: 🚀 触发仓库执行编译
        if: steps.compare.outputs.trigger == 'true'
        uses: peter-evans/repository-dispatch@v2
        with:
          token: ${{ secrets.ACTIONS_TRIGGER_PAT }}
          event-type: Tag_updated
          client-payload: >-
            {
              "tag": "${{ steps.get_tag.outputs.latest_tag }}",
              "source": "auto-check",
              "timestamp": "${{ github.run_id }}"
            }

      - name: 💾 保存本次最新标签
        if: steps.compare.outputs.trigger == 'true'
        run: |
          CACHE_DIR="$HOME/.repo_cache"
          mkdir -p $CACHE_DIR
          CACHE_FILE="$CACHE_DIR/last_tag"
          echo "${{ steps.get_tag.outputs.latest_tag }}" > "$CACHE_FILE"